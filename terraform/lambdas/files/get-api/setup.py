def __init__(self, region, bucket, access_key, secret_key, latest_folder):
    self.apifile_region = "us-east-1"
    self.folder = latest_folder
    self.region = region
    self.bucket = bucket
    self.access_key = access_key
    self.secret_key = secret_key
